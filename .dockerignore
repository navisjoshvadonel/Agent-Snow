# Dependencies — container installs its own clean copy
node_modules
npm-debug.log*

# Git history — not needed at runtime
.git
.gitignore

# Local secrets — never bake into the image
.env

# Dev / editor noise
.vscode
.idea
*.log
*.tmp
